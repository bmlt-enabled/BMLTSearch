// Capacitor serves a static bundle from the device — no server, so everything is
// prerendered at build time and runs entirely client-side thereafter.
export const prerender = true;
export const ssr = false;
